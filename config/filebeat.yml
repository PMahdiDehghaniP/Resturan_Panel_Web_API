filebeat.inputs:
  - type: filestream
    id: app-logs
    paths:
      - /logs/logs.log
    parsers:
      - ndjson:
          target: ""
          overwrite_keys: true

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

setup.kibana:
  host: "http://kibana:5601"